#calculate area  of a circle
radius = input("enter radius (m):")
area = 3.14*int(radius)**2
""" input stores data as a string (>>>(type(10) != type('10')) so, trun radius into integer using `int()` this is called typecasting """



import math
pi = math.pi
radius = input("enter radius (m):")
area = pi*int(radius)**2
